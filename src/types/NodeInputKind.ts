export type NodeInputKind =
  "text" | "number" | "textarea" | "select" | "file" | "credentials" | "none";

export const NodeInputKindValues: NodeInputKind[] = [
  "text",
  "number",
  "textarea",
  "select",
  "file",
  "credentials",
  "none",
];
